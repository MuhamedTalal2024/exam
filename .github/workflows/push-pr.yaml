name: Workflow for Webhook Triggering

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'

jobs:
  webhook_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Tests
        run: |
          ls -la
          npm test