name: scheduled trigger workflow

on:
  schedule:
    - cron: "* * * * *"

jobs:
  schedule_job:
    runs-on: ubuntu-latest

    steps:
      - name: echo message
        run: |
          echo "Hello Muhamed"
          echo "Hello Talal"
